# Generated by `pnpm run css`. Prettier would reformat the minified output and
# the next build would revert it, so the CI freshness check could never pass.
public/app.css

# Generated by pnpm in its own style; rewriting it would fight the package
# manager, and the next install would revert it.
pnpm-lock.yaml

# Build output and dependencies
bin
node_modules
e2e/test-results
e2e/playwright-report

# Impeccable critique archives are generated snapshots, not source.
.impeccable/critique
